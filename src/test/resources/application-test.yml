spring:
  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: none

  jpa:
    properties:
      hibernate:
        default_schema: reservation_service

internal:
  auth:
    secret: test-secret-key-test-secret-key-test-secret-key
