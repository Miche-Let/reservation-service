spring:
  kafka:
    bootstrap-servers: localhost:9092
  jpa:
    hibernate:
      ddl-auto: create

internal:
  secret: ${INTERNAL_AUTH_SECRET}
  auth:
    secret: ${INTERNAL_AUTH_SECRET}

waiting:
  stub: false

timeslot:
  stub: false

kafka:
  stub: true
